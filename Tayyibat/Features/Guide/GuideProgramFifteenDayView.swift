import SwiftUI

/// ٠٦ — برنامج ١٥ يوم. الواجهة الفعلية في `Program15DayView`؛ هذا غلاف رفيع
/// كي يبقى مرجع `GuideSection.program15` ثابتاً.
struct GuideProgramFifteenDayView: View {
    var body: some View {
        Program15DayView()
    }
}
