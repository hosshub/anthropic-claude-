name: tayyibat
description: تطبيق الطيبات — متابعة غذائية واعية (نسخة Flutter).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # الواجهة الخلفية
  supabase_flutter: ^2.5.6
  http: ^1.2.0

  # التقاط الصور
  image_picker: ^1.1.1

  # إدارة الحالة
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
